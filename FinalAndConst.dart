main(){
  final nombre = 'Karoll';
  const pi = 3.1416;

  // Calculo del radio de una circunferenciia: radio = circunferencia/(2*pi)

  const divisor = 2*pi;
  var radio = 15 / divisor;

  print(nombre);
  print('Radio = '+radio.toString());
}